services:

  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: aidev
      POSTGRES_USER: aidev
      POSTGRES_PASSWORD: aidev
    ports:
      - "${DB_HOST_PORT:-15432}:5432"